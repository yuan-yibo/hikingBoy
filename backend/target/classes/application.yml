server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: hiking-record
  
  # 数据源配置
  datasource:
    url: jdbc:mysql://locammend-db-dev-1.c1ue668o8cg6.us-east-2.rds.amazonaws.com:3306/db_hikingboy?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin_dev
    password: 369Shenjianhero!
  
  # Flyway 数据库版本管理
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    validate-on-migrate: true

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  type-handlers-package: com.hiking.infrastructure.persistence.typehandler

# Swagger/OpenAPI 配置
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

# 日志配置
logging:
  level:
    com.hiking: DEBUG
    com.baomidou.mybatisplus: DEBUG

# AI 配置
ai:
  enabled: true
  api-url: https://api.openai.com/v1/chat/completions
  api-key: ${OPENAI_API_KEY:sk-your-api-key-here}
  model: gpt-3.5-turbo
  temperature: 0.8
  max-tokens: 500
  timeout: 30
