# 云开发配置说明

## 开通云开发

1. 打开微信开发者工具
2. 点击菜单栏 "云开发" -> "开通云开发"
3. 选择或创建云环境
4. 确认开通

## 数据库集合

已创建以下数据库集合：

- `hikingRecords` - 存储爬山记录数据

## 云存储

- 图片将上传到云存储，路径为 `hiking-photos/`

## 注意事项

- 首次使用需要在开发者工具中开通云开发
- 确保在 `project.config.json` 中配置了正确的 `cloudfunctionRoot`
- 确保在 `app.json` 中设置了 `"cloud": true`